<apex:page standardController="Candidate__c" extensions="appinfo1">
    <apex:form >
        <apex:pageBlock >
            <apex:pageBlockTable value="{!jobapp}" var="allappinfo">
                <apex:column >
                    <apex:outputField value="{!allappinfo.Position__r.name}"/>
                </apex:column>
                <apex:column >
                    <apex:outputField value="{!allappinfo.Position__c}"/>
                </apex:column>
            </apex:pageBlockTable>
        </apex:pageBlock>
    </apex:form>    
  </apex:page>